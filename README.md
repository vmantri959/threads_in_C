Simple program that demonstrates the use of threads, conditional variables and updating atomic variables.

**Build Instructions:**

```bash
cmake -S . -G "Unix Makefiles" -B build
cmake --build build
./build/practicing_threads

